<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".TrainvocApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trainvoc"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Trainvoc">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Notification Action Receiver for handling notification button clicks -->
        <receiver
            android:name=".notification.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.gultekinahmetabdullah.trainvoc.ACTION_I_KNOW" />
                <action android:name="com.gultekinahmetabdullah.trainvoc.ACTION_SHOW_ANSWER" />
                <action android:name="com.gultekinahmetabdullah.trainvoc.ACTION_SKIP" />
                <action android:name="com.gultekinahmetabdullah.trainvoc.ACTION_GOT_IT" />
                <action android:name="com.gultekinahmetabdullah.trainvoc.ACTION_NEED_PRACTICE" />
                <action android:name="com.gultekinahmetabdullah.trainvoc.ACTION_NEXT" />
            </intent-filter>
        </receiver>

        <!-- Streak Widget -->
        <receiver
            android:name=".widget.StreakWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.gultekinahmetabdullah.trainvoc.ACTION_UPDATE_STREAK_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_streak_info" />
        </receiver>

        <!-- Daily Goals Widget -->
        <receiver
            android:name=".widget.DailyGoalsWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.gultekinahmetabdullah.trainvoc.ACTION_UPDATE_GOALS_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_daily_goals_info" />
        </receiver>

        <!-- Disable default WorkManager initialization since we use on-demand initialization -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="remove" />
    </application>

</manifest>