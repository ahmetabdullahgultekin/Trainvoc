# Trainvoc Backend - Docker Environment Variables
# ================================================
# Copy to .env and fill in values before running docker-compose
#
# Usage:
#   cp docker.env.example .env
#   # Edit .env with your values
#   docker-compose up -d

# ===========================================
# Database Configuration
# ===========================================

# Primary Database (trainvoc - game data)
DB_USER=trainvoc
DB_PASSWORD=your_secure_password_here

# Words Database (trainvoc-words - vocabulary)
DB_WORDS_USER=trainvoc
DB_WORDS_PASSWORD=your_secure_password_here

# ===========================================
# Spring Configuration
# ===========================================

# Profile: dev, prod
SPRING_PROFILE=dev

# ===========================================
# CORS Configuration
# ===========================================

# Development:
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Production (uncomment and update):
# CORS_ORIGINS=https://trainvoc.rollingcatsoftware.com,https://www.trainvoc.rollingcatsoftware.com

# ===========================================
# Production Only (Optional)
# ===========================================

# SSL Certificate paths (if using Nginx)
# SSL_CERT_PATH=/etc/letsencrypt/live/api.trainvoc.rollingcatsoftware.com/fullchain.pem
# SSL_KEY_PATH=/etc/letsencrypt/live/api.trainvoc.rollingcatsoftware.com/privkey.pem
